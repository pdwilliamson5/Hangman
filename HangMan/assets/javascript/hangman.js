<!DOCTYPE html>
<html>
<head>

<


var randomwordArr = ["beer", "wine", "brewery", "alcohol","pizza"];

var randomWord = randomwordArr[math.floor(Math.random()] * randomwordArr)];



var s;

var count = 0;

var answerArray = [];

function startUp()
{
	for ( var i = 0; i < randomWord.length; i++)
	{
		answerArray[i] = "_"
	}
}